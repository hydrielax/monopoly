# Monopoly

To execute:

```sh
mvn exec:java
```

To test:

```sh
mvn test
```

To generate docs:

```sh
mvn javadoc:javadoc
```

To compile jar with dependencies:

```sh
mvn compile assembly:single
```
